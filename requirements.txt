langgraph>=0.2.50
langchain>=0.3.0
langchain-core>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
networkx>=3.2
httpx>=0.27.0
python-dotenv>=1.0.1
rich>=13.7.0

# Layer 3 selectivity model + Layer 4 figures (Tier-0 runs anywhere, no GPU)
numpy>=1.26
matplotlib>=3.8

# Optional LLM provider integrations (install the one you use)
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0

# Optional Tier-1 foundation-MLIP surface reactivity (Colab CUDA / CPU):
#   pip install "ase>=3.23" "mace-torch>=0.3.6"
# Optional Tier-2 semi-empirical calibration:
#   pip install "tblite>=0.3.0"
